Follow the skill at `.agents/skills/security-scan.md` to run all security scans locally.

Read the full skill file first, then execute each step interactively with the user.
Auto-detect local vs external SonarQube mode based on SONAR_TOKEN environment variable.
