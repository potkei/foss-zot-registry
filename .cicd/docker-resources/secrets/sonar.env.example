# SonarQube credentials — EXAMPLE ONLY
# Copy to sonar.env (gitignored) and fill in real values for external mode.
# Leave empty to use local stack (docker-compose.scan.yml auto-setups everything).

SONAR_HOST_URL=https://sonar.company.com
SONAR_TOKEN=sqp_REPLACE_WITH_YOUR_TOKEN
