# Image signing key reference — EXAMPLE ONLY
# Real key stored in your secrets manager. Never commit real keys.

# Cosign key reference (for CI injection):
COSIGN_KEY=vault:secret/cosign/private-key
# Or path to key file (mounted at runtime):
COSIGN_KEY_FILE=/run/secrets/cosign.key
COSIGN_PASSWORD=REPLACE_ME
